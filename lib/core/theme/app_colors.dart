import 'package:flutter/material.dart';

/// Standard app colors for CashRound
class AppColors {
  AppColors._();

  /// Primary color - vibrant orange
  static const Color primary = Color(0xFFF87B1B);

  /// Secondary color - deep navy blue
  static const Color secondary = Color(0xFF11224E);

  /// Ancient color - pure white (background/light elements)
  static const Color ancient = Color(0xFFFFFFFF);

  /// Variant color - light gray (secondary backgrounds, dividers)
  static const Color variant = Color(0xFFEBEBEB);
}
